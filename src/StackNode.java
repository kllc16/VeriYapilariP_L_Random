class StackNode {
    int data;           // Veri (basit örnek için int)
    StackNode next;     // Bir ALTTAKİ node (aşağıya işaret)

    StackNode(int data) {
        this.data = data;
        this.next = null;
    }
}
